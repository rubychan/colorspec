require 'spec_helper'

describe ColorSpec do
  it 'has a version number' do
    expect(ColorSpec::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
